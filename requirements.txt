google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.131.0
google-genai>=2.10.0
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
groq>=1.5.0
ddgs>=0.1.0
python-dotenv>=1.0.1
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
cryptography>=42.0.0
itsdangerous>=2.2.0
werkzeug>=3.0.0
pillow>=10.0.0
paddlepaddle>=2.6.0
paddleocr>=2.7.0
apscheduler>=3.10.4
firebase-admin>=6.5.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Website Sale Scraper (services/website_scraper/)
selectolax>=0.3.27
extruct>=0.18.0
# Headless-browser fallback for sites whose WAF/bot-mitigation blocks plain
# HTTP requests (see services/website_scraper/crawler.py::fetch_html_browser).
# Requires `playwright install chromium` once after `pip install`.
playwright>=1.49.0
# Akamai Bot Manager (adidas.com and similar) still 403s Playwright Chromium;
# Camoufox is the Firefox-fingerprint fallback used after that.
camoufox[geoip]>=0.4.0

# Testing (tests/) — httpx is what fastapi.testclient.TestClient uses internally
pytest>=8.0.0
pytest-mock>=3.14.0
httpx>=0.27.0
